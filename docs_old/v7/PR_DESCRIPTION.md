## v7 디자인/플로우 시안 (디자인 A 인디고)

### 보는 곳
- 로컬: `npm run dev` → http://localhost:3199/v7
- (Vercel preview는 인증 보호 중 — 디자인 결정 후 공개 예정)

### 완성된 것 ✅
- 5스텝 선형 플로우 + 전환 모두 작동:
  ① 온보딩 대화 → ② 특징 정리(CONTENT PROFILE)
  → ③ 트렌드 → ④ 라이벌+최근영상 → ⑤ 스크립트/콘티 → ③ 복귀
- 흰 배경 + 인디고, 풀스크린 (기존 다크 셸 없음)
- 데모 v7-A-indigo 기준으로 구현

### 아직 mock인 것 ⚠️ (디자인 확정 후 실데이터 연결 예정)
- 모든 데이터가 정적 mock (실제 API/LLM 미연결)
- /v7은 "디자인+플로우 검증용 시안"
- 실데이터 연결 시 필요:
  - ② 페르소나 ← persona API (키 필요)
  - ③ 트렌드 ← /api/trends (이미 작동, 연결만)
  - ④ 라이벌 ← creator-recommend (연결만)
  - ⑤ 제작 ← /api/generate·conti (Gemini 키 필요)

### 팀 결정 요청
1. 디자인 방향: A(인디고) / B(볼드) / C(퍼플) 중?
2. 5스텝 플로우 흐름 OK?
3. 결정되면 → 실데이터 연결 → main 머지 진행
